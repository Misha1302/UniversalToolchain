# Legacy test inventory

All files in this project are currently classified as `legacy`.

This project holds the historical/quarantined suites that were previously hidden from `UniversalToolchain/Tests/Tests.csproj`
via `<Compile Remove="...">` entries.

Moved inventory:

- `Core/*.cs`
- `Internal/*.cs`
- `Stress/*.cs`
- `Legacy/*.cs`
