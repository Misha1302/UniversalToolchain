<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <LangVersion>14</LangVersion>
        <IsPackable>false</IsPackable>
        <Description>S11 compatibility shell for the retired UniversalToolchain.Dialects.Wist project identity. It owns no runtime components, emits no runtime manifest, and must not be referenced by canonical Wist production projects.</Description>
    </PropertyGroup>

</Project>
