# TemplateLanguage

A standalone non-Wist UniversalToolchain language generated by `dotnet new ut-language`.
The package descriptor and runtime implementation come from one typed authoring definition.

Run `dotnet run`; expected output is `42`.
