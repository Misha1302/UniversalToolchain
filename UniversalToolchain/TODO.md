# TODO - ideas to implement in the project
## 1. Dependency Injection
DI is the big part of the project's architecture. 
In this moment DI builds from assemblies (dlls) which are located in subdirectories of executing app (dll).
It's bad for the predictibility and deterministics.
We need to make some another DI logic, which will:
1. help to lower to zero components dependencies
2. be predictable and deterministic

## 2. Remove extra boiel-plate code
1. DynamicMethodInvoker can be generated by python script OR roslyn-code-generator

## 3. Find logical difference between interpreter and compiler
1. Fix difference
2. Make interfaces or other API for platform-independent features 
3. For platform-dependent features make API for current platforms

## 4. Fix intrinsics
1. Make something like a 'center' to manipulate intrinsics
2. Make simple API for modules to not generate invalid intrinsic

## 5. Fix BasicCoreImpl
1. Fix abstractions and it's leaks
2. Add an opportunity to pass arguments and parameters into compilers, executors, etc (-O3, -pgo, ...).
3. Fix abstractions and logical separation for code parameters - it's very-very-very bad in current moment (maybe make new module? - GetCodeWithParametersDefault)
4. Add more extensibility (for example add new modules)
5. Replace OrderedDictionary to something normal
6. Find another abstractions leaks

## 6. Fix modules abstractions
1. Add (reversed?) topology sort between modules
2. Unite modules to groups
3. Add dependencies between modules/groups and it's order
4. Import groups of modules
5. Add multilevel groups (group of groups for many levels)
6. Add dialects (maybe list of groups/modules)

## 7. Add normal thrower service (maybe via DI?)
1. Add helpful messages for all exceptions/errors
2. Add semantic and sintax checkers

## 8. Fix configurations
1. Use csv/json/yaml/another format instead of current txt

## 9. Write summary
1. Add **big/helpful** summary for classes, modules, groups
2. Add **short** summary for methods/functions
3. Add docs.md (or another name) for documentation of difficult alghorimts 

## 10. Fix projects
1. Rename if need
2. Move projects to new folders

## 11. Implement CIL optimizer
1. Very important feature is to inline interfaces
2. Add pgo and generate faster cil methods
3. Build SSA and optimize it
4. Add basic optimizations (e.g., remove ldloc.X, stloc.X)

## 12. Use reflection only via one 'center' (DI or singletone)

## 13. Analize code-base for bugs

## 14. Add sandbox execution
1. Import only safety modules
2. Add reflection's restrictions

## 15. Fix small abstractions leaks
1. Add @ to identifier definition, not to label (or use reversed logic)

## 16. Split tests to groups, folders

## 17. Move ALL usings to global usings

## 18. Think about frontend remade
1. Make DSL or use Antrl (some another) to use different parsers (O(n), O(2^n), ALL(*), LR(), etc)
2. Think about runtime extensibility

## 19. Fix git
1. Remove all.txt, partial_main.txt, other txt with code from git
2. Move to gitignore
3. Update readme and the site