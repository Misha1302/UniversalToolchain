# Future work

- Add the frozen-style E4 downstream control in a new study revision, not by rewriting this measurement cycle.
- Repeat with 3-5 independently useful variants to look for a total-cost crossover rather than only E2 marginal-cost crossover.
- Use an external clone detector or token/AST clone detector alongside the current transparent approximation.
- Create evolution-step commits that are independently runnable for both treatments, enabling M1/M2/CAR at every step.
- Evaluate a real syntax/grammar extension once the generic authoring surface supports it without benchmark-specific framework work.
- Add a second independently authored subject to reduce sensitivity to the pricing micro-language design.
- Compare against a strengthened registry/shared-operation baseline explicitly positioned between clone-and-own and a full composition platform.
