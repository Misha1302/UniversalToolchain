---
title: Wist 0.1.0-alpha.4 Immutable Pruning
audience: Maintainers and package consumers
status: verified
lastVerifiedAgainst: 0.1.0-alpha.4
---
# Wist 0.1.0-alpha.4 immutable pruning release

This release advances the package identity after the runtime package closure was pruned. It does not reuse the previously distributed `0.1.0-alpha.3` identity.

The release path requires the real previous source archive for API comparison and the real previous package bundle for monotonic package-version validation. Packages are rewritten into a deterministic ZIP representation, and two clean builds from different absolute paths must produce identical package hashes before delivery.

The canonical test contract is **1544 passed, 0 failed, 0 skipped**.
