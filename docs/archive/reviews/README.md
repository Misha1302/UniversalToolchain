# Archived reviews

This directory contains reviews and broad project snapshots that may be useful
for history but are not canonical current-state documentation.

Use `docs/CURRENT_ARCHITECTURE_STATUS.md` first when current behavior matters.

