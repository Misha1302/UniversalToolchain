# Flame SSA optimizing backend proposal moved

The Flame SSA optimizing backend design was moved to
`docs/proposals/flame-ssa-optimizing-backend-design/`.

It remains a proposal and is not current runtime behavior by default.

