# Typed module contracts proposal moved

The typed module contracts and verifiers design was moved to
`docs/proposals/typed-module-contracts-and-verifiers.md`.

It is a proposal unless a current-state document explicitly says that a specific
part has been implemented.

