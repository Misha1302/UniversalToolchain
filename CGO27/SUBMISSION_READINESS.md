# CGO 2027 submission readiness

Current verdict: `CONDITIONAL_GO`.

The paper and anonymous supplementary package are suitable for submission only with the declared claim boundaries below and only after the current branch head has a complete green exact-head provider receipt. This verdict does not authorize merge, publication, or conference submission.

## Completed submission gates

- bounded formal guarantee, assumptions, implementation owners, and proof sketch agree;
- the strongest demand-driven alternative is executable and evaluated with a matched queried/unqueried counterexample;
- versioned boundary, source-to-result, public-SDK-language, and ablation studies regenerate their tables from raw evidence;
- P07 is repaired independently without deleting the case or changing its oracle;
- historical screening has complete frozen accounting: 24 candidates = 3 included + 11 excluded + 10 resource/provenance blocked;
- the anonymous Letter manuscript is ten total pages, has embedded fonts, no undefined references or overfull horizontal boxes, and is visually readable without color;
- the manuscript includes a strongest-alternative comparison, explicit limitations, and an anonymous Generative AI usage disclosure;
- the deterministic anonymous supplement passes clean quick/full replay, manifest validation, table regeneration, and identity scanning;
- three model-authored adversarial review passes have no unresolved blocking scientific, evidence, anonymity, or format finding.

## Declared non-claims and external conditions

- `BLOCKED_EXTERNAL`: no externally authored frozen corpus; therefore no independent/external-validity claim;
- `BLOCKED_PINNED_MACHINE`: no pinned performance host; therefore no whole-compilation speedup claim;
- `BLOCKED_RESOURCE`: the new exact pre-fix historical replay did not reach policy execution under the available resource envelope; therefore no historical P2 detection-rate claim;
- P2/P3 parity is empirical on the evaluated corpora, not a universal equivalence theorem;
- public-project de-anonymization risk cannot be eliminated; the anonymous package reduces avoidable direct identifiers but cannot make motivated search impossible.

## Required final provider receipt

The exact branch head used for delivery must have all required workflows green on the same revision: canonical .NET tests, contract experiment, source-to-result experiment, Language T, ablations, paper, archival artifact, anonymous supplement, documentation/architecture checks, benchmark smoke, rollout smoke, and package compatibility. The PR description and delivered readiness report record the resulting run and artifact identities.

Merge, direct `master` push, NuGet publication, public supplementary publication, author-list changes, and actual conference submission remain forbidden without separate explicit approval.
